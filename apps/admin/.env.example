NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WEB_URL=http://localhost:3000

# Auth modes: staff | firebase
# - staff: POST /v1/auth/staff-login (development/staging only; production builds fail)
# - firebase: required for production admin builds once staff Firebase is wired
NEXT_PUBLIC_AUTH_MODE=staff
