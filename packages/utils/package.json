{
  "name": "@autohub/utils",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test:listing-draft": "pnpm dlx tsx --test src/listing-draft/engine.spec.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
